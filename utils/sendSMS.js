// src/utils/sendSMS.js
module.exports = function sendSMS(phoneNumber, message) {
    // Placeholder implementation: log the SMS content
    console.log(`📱 Sending SMS to ${phoneNumber} | Message: ${message}`);
    // In production, integrate with an SMS gateway API (e.g., Twilio)
};
